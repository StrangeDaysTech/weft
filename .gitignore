# ---- Rust (native/weft-ffi) ----
/target/
**/target/
**/*.rs.bk
*.pdb
# Cargo.lock SÍ se versiona (research R16): reproducibilidad del gate de determinismo y del cdylib.
# Artefactos de cargo-fuzz (el corpus/artifacts no se versionan):
**/fuzz/corpus/
**/fuzz/artifacts/

# ---- .NET ----
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/
project.lock.json
artifacts/
*.nupkg
*.snupkg

# runtimes nativos generados/copied (se producen en CI por RID)
runtimes/*/native/*.so
runtimes/*/native/*.dll
runtimes/*/native/*.dylib

# ---- Herramientas / entorno ----
.idea/
.vscode/
*.log
.DS_Store
Thumbs.db

# ---- Spec Kit (mantener specs; ignorar temporales si los hubiera) ----
# .specify/  -> se versiona (spec/plan/tasks son fuente de verdad)

# Artefactos temporales de CLIs auditores (audit-execute)
**/pending-skills/
