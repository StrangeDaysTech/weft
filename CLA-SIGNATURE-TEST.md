# PRUEBA — flujo de firma CLA v2

Archivo desechable para verificar que el flujo de firma escribe en el registro central
`StrangeDaysTech/cla-registry` (`weft/signatures/version2/cla.json`) con enriquecimiento
`cla_version` + `cla_sha256`. Este PR **no se mergea**; se cierra tras la verificación.
