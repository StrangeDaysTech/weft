name: CLA Assistant

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  CLAAssistant:
    runs-on: ubuntu-latest
    steps:
      - name: CLA Assistant
        # Solo actúa sobre PRs o sobre el comentario de firma en un PR (no en issues normales).
        if: >-
          (github.event_name == 'issue_comment' &&
           contains(github.event.comment.body, 'I hereby sign the CLA')) ||
          github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Documento que el firmante debe leer (vive en main tras el merge de este PR de setup).
          path-to-document: 'https://github.com/StrangeDaysTech/weft/blob/main/CLA.md'
          # Las firmas NO pueden ir a main (el ruleset exige PR); rama dedicada no protegida.
          branch: 'cla-signatures'
          path-to-signatures: 'signatures/version1/cla.json'
          # Texto exacto que el contribuidor comenta para firmar.
          custom-pr-sign-comment: 'I have read the CLA Document and I hereby sign the CLA'
          # Bots exentos de firmar.
          allowlist: 'dependabot[bot],renovate[bot],github-actions[bot]'
